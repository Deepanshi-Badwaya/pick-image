import 'package:flutter_test/flutter_test.dart';

import 'package:pick_image/pick_image.dart';

void main() {
  test('Pick image from camera and gallery', () {
    final pickImage = ImagePickerComponent();
    pickImage;
  });
}
